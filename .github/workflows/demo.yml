name: demo

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  one-command-demo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and run
        run: ./demo
